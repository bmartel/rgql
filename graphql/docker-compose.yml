version: "3.4"
services:
  # Postgres DB
  db:
    image: postgres:12
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  api:
    image: dosco/graphjin:latest
    environment:
      GO_ENV: "development"
    volumes:
      - ./config:/config
    ports:
      - "9071:9071"
    depends_on:
      - db
